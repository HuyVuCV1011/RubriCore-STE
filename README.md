# RubriCore-STE

Starter repository for the RubriCore STE project.

## Getting Started

This repository is initialized locally and ready to connect to a remote named `origin`.

```sh
git remote add origin <remote-url>
git branch -M main
git push -u origin main
```

Replace `<remote-url>` with your GitHub, GitLab, or other Git remote URL.

